# GUI / ресурсы

## Поиск Unit по имени в Manager

`Reference.name` — снимок для восстановления битой ссылки, не кэш для UI.

Мечта: у Manager (или рядом) — поиск Unit по имени; при нескольких совпадениях — ImGui-выпадашка, пользователь выбирает, чем чинить `id`.
