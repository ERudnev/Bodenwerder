import "system/core"
import "system/viewInput"
import "scene/camera"

namespace rmmr
  namespace controller

    // Ortho/2D pan control for an existing scene::Camera.
    // Driven once per frame by the engine input phase (=tick); input from its ViewInput mail.
    attribute Camera2d of scene::Camera
      one
        input: #system::ViewInput
        >create(input: #system::ViewInput)-> #
      all
        =tick(dt: seconds) // RMB drag + arrows on XY
