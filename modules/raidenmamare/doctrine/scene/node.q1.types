import "../math"

namespace rmmr
  namespace scene

    // Basic transformable scene node.
    entity Node
      one
        pose: Pose
        ?transform() -> mat4
      all
        >create(Locator) -> # // convenience c-tor
