#pragma once

#include <fQSM/meta/interface.include.h>

namespace fqsm::state::connections {

    struct Abstract {
        virtual ~Abstract() = default;
    };

    //template<meta

    struct World {
    };
}