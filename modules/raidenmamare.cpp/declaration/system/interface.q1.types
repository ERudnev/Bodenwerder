import "core"
import "window"

namespace rmmr
  namespace system

    manipulation Interface of Core
      >create(path: string, version: Core::GLVer)->#
      >createWindow(title: string, requested_size: index2)->#Window
      >shutdown()
