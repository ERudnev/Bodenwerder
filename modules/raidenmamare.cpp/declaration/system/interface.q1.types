import "core"
import "window"

namespace rmmr
  namespace system

    manipulation Interface of Core
      >create(path: string, version: Core::GLVer)->#
      >createWindow(title: string, size: index2)->#Window
      >shutdown()
