import "../system/core"

namespace rmmr
  namespace controller

    component Dispatcher of system::Device
      one // literally this is "input state"
        clock: double
        keys: vector<bool> // simplified for now
        mouse: index2
        =update(now: seconds)
