import "../math"
import "node"

namespace rmmr
  namespace scene

    // Camera settings bound to a scene node.
    feature Camera of Node
      one
        fov_y: float
        z_near: float
        z_far: float
        ?projection(aspect_ratio: float) -> mat4
        ?view() -> mat4
        ?view_projection(aspect_ratio: float) -> mat4
      all
        >create(Locator, fov_y: float, z_near: float, z_far: float) -> #
