import "../math"

namespace rmmr
  namespace scene

    // Basic transformable scene node.
    entity Node
      one
        position: Pos
        rotation: quat
        ?transform() -> mat4
        ?hpb() -> HPB // heading/pitch/bank (degrees)
        =hpb(HPB)
      all
        >create(Locator) -> # // convenience c-tor
