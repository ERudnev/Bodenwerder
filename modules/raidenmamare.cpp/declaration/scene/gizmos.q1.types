import "../resources_old/geometry"
import "../resources_old/material"
import "../renderer/types"
import "../math"
import "node"

namespace rmmr
  namespace scene

    feature Grid of Node
      one
        geometry: #resource_old::Geometry
        material: #resource_old::Material
        opacity: float
        ?submit(>where: renderer::CommandBuffer)
      all
        >create(Pos, HPB, one) -> #
