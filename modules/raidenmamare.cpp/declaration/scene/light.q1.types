import "../math"
import "../resources/shadows"
import "node"

namespace rmmr
  namespace scene

    // Simple point light bound to a scene node.
    feature Light of Node
      one
        color: RGB
        intensity: float
        range: float
        shadow: #resource::shadow::Asset?
      all
        >create(Locator, RGB, intensity: float, range: float) -> #
