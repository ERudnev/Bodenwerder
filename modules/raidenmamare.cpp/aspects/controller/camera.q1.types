import "controller/dispatcher"
import "scene/camera"

namespace rmmr
  namespace controller

    attribute Camera of Dispatcher
      element
        camera: #scene::Camera @anchor
        >create(): # // creates controller::Dispatcher on the camera node (scene #scene::Camera already exists there), then this attribute; input/update flows through Dispatcher global
        =update(now: seconds) // RMB + mouse: rotate anchored scene::Camera node (HPB)
