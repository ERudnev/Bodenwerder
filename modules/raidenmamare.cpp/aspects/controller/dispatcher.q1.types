import "device"

namespace rmmr
  namespace controller
    attribute Dispatcher of scene::Node
      table // literally this is "input state"... dont worry, this is implicit design.
        device: #Device?
        clock: double
        keys: vector<bool> // simplified for now
        mouse: index2
        active: uset<#>
        =update(now: seconds)
