namespace rmmr
  namespace scene

    // Basic transformable scene node.
    // Rendering payloads (primitive, camera, light, viewport bindings) can be layered later.
    entity Node
      element
        position: vec3
        rotation: quat
        ?transform()->mat4
        ?euler()->vec3 // heading/pitch/bank
        *euler(vec3)
