import "math"

namespace rmmr
  namespace scene

    // Basic transformable scene node.
    // Rendering payloads (primitive, camera, light, viewport bindings) can be layered later.
    entity Node
      one
        position: Pos
        rotation: quat
        ?transform() -> mat4
        ?hpb() -> HPB // heading/pitch/bank (degrees)
        =hpb(HPB)
      all
        >create(pos: Pos, hpb: HPB) -> # // convenience c-tor
