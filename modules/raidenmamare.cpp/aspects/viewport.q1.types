import "device"

namespace rmmr

  // Viewport configuration for rendering into Device's default framebuffer.
  // Later this can grow camera / render-target semantics without bloating Device.
  entity Viewport
    one
      device: #Device
      origin: index2
      size: index2
      clear_color: vec4
      ?activate()
      ?clear()
