import "rmmr/math"
import "physics/particle"

namespace eltanin
  namespace phys

    entity Atomic
      struct Rest
        centered: vector<vec3>
        com: vec3
      one
        particles: vector<#phys::Particle>
        rest: Rest
        restored: rmmr::Pose
        =debugAddImpulse(imp: vec3)
      all
        *satisfy(~Particle) // Horn shape-matching: pull particles to restored base pose
