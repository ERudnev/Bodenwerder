import "rmmr/resources/runtimes"

namespace eltanin
  namespace resource

    using Unit as rmmr::resource::Unit

    // Story-owned asset entry points (plate packs, frames, …) — not rmmr primitives.
    // Extend with >add_* as concrete asset kinds appear.
    component Assets of rmmr::resource::Assets
      one
