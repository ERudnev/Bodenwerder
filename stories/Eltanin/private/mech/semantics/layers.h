#pragma once

namespace eltanin::mech {

    // Level 0 — how a piece lives on the ship (not form, not function).
    enum class layer {
        frame,
        hull,
        internals,
        externals,
    };

}
